import 'package:flutter/material.dart';

class Assarti extends StatelessWidget {
  const Assarti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}