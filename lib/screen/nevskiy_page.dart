import 'package:flutter/material.dart';

class NevskiyPage extends StatelessWidget {
  const NevskiyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("data"),
      ),
    );
  }
}
